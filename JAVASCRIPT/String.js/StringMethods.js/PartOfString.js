let str="There is a protest going on the jantar mantar in delhi city.";

//str.slice(start, end)  method extracts a part of a string and returns the extracted part in a new string.

console.log(str.slice(0, 5)); // "There"
console.log(str.slice(10, 17)); // "protest"
