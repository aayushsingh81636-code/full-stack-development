let firstname = `Aayush`;
let lastname = `Singh and my aim is to become a full stack developer.`;
/*let fullname = firstname+lastname;*/
   // console.log(fullname);
   
/** This will log "AayushSingh" because the + operator concatenates the two strings */
/** Template literals allow for multi-line strings and string interpolation */
console.log(`Hello,My name is ${firstname} ${lastname}`);
